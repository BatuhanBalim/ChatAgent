package com.example.chatbot.data.api

/**
 * Contains API-related constants
 */
object ApiConstants {
    // TODO: Replace this with your actual OpenAI API key before building the app
    // IMPORTANT: Be aware that embedding API keys in your app can lead to:
    // 1. Potential misuse by others (costs incurred on your account)
    // 2. Security risks (if app is decompiled)
    // 3. Rate limiting issues if many users access with same key
    const val OPENAI_API_KEY = ""
}
